class Solution(object):
    def reverse(self, x):
        """
        :type x: int
        :rtype: int
        """
        i = 0 
        j = len(x)-1
        while(i<j):
            x[i],x[j] = x[j],x[i]
            i +=1
            j -= 1
